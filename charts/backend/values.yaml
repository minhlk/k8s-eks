namespace: example-namespace
replicaCount: 1

image:
  repository: minhlk/k8s-backend
  port: 9000
  tag: "1.0.0"

service:
  type: ClusterIP
  port: 80

resources: {}

metrics:
  target:
    averageUtilization: 40